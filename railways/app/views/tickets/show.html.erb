<p>
  <strong>Ticket number:</strong>
  <%= @ticket.id %>
</p>

<p>
  <strong>Passenger's full name:</strong>
  <% if @ticket.passenger_full_name.present? %>
    <%= @ticket.passenger_full_name %>
  <% else %>
    Not specified
  <% end %>
</p>

<p>
  <strong>Passenger's passport:</strong>
  <% if @ticket.passport.present? %>
    <%= @ticket.passport %>
  <% else %>
    Not specified
  <% end %>
</p>

<p>
	<strong>For the train:</strong>
	<% if @ticket.train.present? %>
		<%= @ticket.train.number %>
	<% else %>
		Not specified
	<% end %>
</p>

<p>
  <strong>From the station:</strong>
  <% if @ticket.first_station.present? %>
    <%= @ticket.first_station.title %>
  <% else %>
    Not specified
  <% end %>
</p>

<p>
  <strong>To the station:</strong>
  <% if @ticket.last_station.present? %>
    <%= @ticket.last_station.title %>
  <% else %>
    Not specified
  <% end %>
</p>

<%= link_to 'Retun to train tickets', train_tickets_path(@ticket.train) %>
