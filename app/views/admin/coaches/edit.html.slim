h1
  | Editing #{"#{@type.underscore.humanize}"}
= render 'form'
br
br
= link_to 'Show', sti_coach_path(nil, @coach.type, @coach), class: "btn btn-sm btn-default"
= link_to 'Back to train', [:admin, @coach.train], class: "btn btn-sm btn-default"
